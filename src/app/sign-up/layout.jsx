export const metadata = {
  title: "Sign Up - Create a True Lens account",
  description: "Create a True Lens account and start fact-checking.",
};

const SignUpLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default SignUpLayout;
