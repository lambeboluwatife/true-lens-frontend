export const metadata = {
  title: "About True Lens - Decipher the Facts",
  description: "True Lens mission and vision statement.",
};

const AboutPageLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default AboutPageLayout;
