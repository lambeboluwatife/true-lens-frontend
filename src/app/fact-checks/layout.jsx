export const metadata = {
  title: "True Lens - Fact Check",
  description: "True Lens recent or most impactful fact-checks.",
};

const FactCheckLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default FactCheckLayout;
