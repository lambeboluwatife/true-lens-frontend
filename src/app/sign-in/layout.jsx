export const metadata = {
  title: "Sign In - Sign In to your True Lens account",
  description: "Sign In to your True Lens account and continue fact-checking.",
};

const SignInLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default SignInLayout;
