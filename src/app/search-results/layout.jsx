export const metadata = {
  title: "True Lens - Search Results",
  description: "Results from user search.",
};

const SearchResultsLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default SearchResultsLayout;
