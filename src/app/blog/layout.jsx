export const metadata = {
  title: "True Lens - Blogs",
  description:
    "Articles and insights on current trends in misinformation and digital literacy.",
};

const BlogsLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default BlogsLayout;
