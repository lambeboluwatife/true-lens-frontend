export const metadata = {
  title: "True Lens - Contact Us",
  description: "We'll love to hear from you and your feedback.",
};

const ContactLayout = ({ children }) => {
  return <div>{children}</div>;
};

export default ContactLayout;
